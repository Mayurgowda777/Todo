# Todo

A static to-do list that runs without a backend or browser storage. Changes are kept in memory and reset when the page is reloaded.

Open `index.html` directly or deploy the project to any read-only static server.
